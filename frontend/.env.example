# AgentWeave frontend 配置示例
# 用法：
# 1. 复制本文件为 frontend/.env
# 2. 如果前端和后端同源部署，这两个变量都可以保持默认值

# VITE_API_BASE_URL
# 含义：前端访问后端 REST API 的基地址。
# 本项目默认值：空字符串。
# 说明：为空时走同源请求；开发分离部署时可填写 http://127.0.0.1:8000
VITE_API_BASE_URL=

# VITE_WS_URL
# 含义：前端建立聊天 WebSocket 的地址。
# 本项目默认值：根据当前页面自动推导为 ws(s)://<当前 host>/ws/chat
# 说明：开发分离部署时可填写 ws://127.0.0.1:8000/ws/chat
VITE_WS_URL=
